opencv-python>=4.9,<5
numpy<2.0; python_version < "3.10"
numpy>=2.1; python_version >= "3.10"
PyYAML>=6.0
Flask>=3.0
flask-cors>=4.0
python-socketio>=5.11,<6

# GPIO — installé uniquement sur Linux (A133). Ignoré sur Windows grâce au marker.
# v2.x nécessite libgpiod 2.x sur le système. Si indisponible, installe plutôt
#   `gpiod<2` pour utiliser la v1 (Ubuntu 20.04 par défaut, libgpiod 1.6).
gpiod>=2.0; platform_system == "Linux"

# Optional alternative embedding backend (dlib) — décommenter pour tester
# Sur Windows requiert CMake + MSVC Build Tools
# face-recognition==1.3.0
# dlib==19.24.2
